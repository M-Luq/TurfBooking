export interface CustomerDTO {
    name: string;
    email: string;
    phoneNumber: string;
    address: string;
  }
  
  export interface Customer {
    name: string;
    email: string;
    phoneNumber: string;
    address: string;
    favouriteTurfs:string;
  }